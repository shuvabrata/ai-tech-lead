# Implementation Plans

Generated by the improve skill on 2026-06-26 from a deep audit of the
`work-behavior-analytics-ai` codebase at commit `ec14dc5`. Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

**Last reconciled**: 2026-06-27 at commit `24c665d`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | [Remove `include_secrets` from public routes](001-remove-include-secrets-from-public-routes.md) | P0 | S | — | TODO |
| 002  | [Fix Cypher injection & read-only sessions](002-fix-cypher-injection-and-read-only-sessions.md) | P1 | S | — | DONE ✓ |
| 003  | [Neo4j driver singleton](003-neo4j-driver-singleton.md) | P1 | S | 002 | DONE ✓ |
| 004  | [Async graph route handlers](004-async-graph-route-handlers.md) | P2 | S | 003 | DONE ✓ |
| 005  | [MCP thread join timeout](005-mcp-thread-join-timeout.md) | P2 | S | — | TODO |

## Reconciliation notes (2026-06-27)

- **002 DONE ✓** — Verified at `24c665d`: `grep -cn "READ_ACCESS" query.py` → 3 matches; `tests/test_graph_query_validation.py` exists and 15/15 tests pass. The `import neo4j` and `default_access_mode=neo4j.READ_ACCESS` changes landed in commit `74a327f` (PR #177).
- **001 TODO** — No drift. `include_secrets` still present in `router.py` lines 62 and 98. Code matches plan excerpts exactly. **This is the highest-priority executable plan (P0 security).**
- **003 TODO** — No drift. `GraphDatabase.driver` still appears 3 times in `query.py`; `driver.close` still appears 3 times; `_get_driver` does not exist. Dependency on 002 is now satisfied — **executable immediately.**
- **004 TODO** — No drift. `asyncio.to_thread` count in `router.py` is 0. Blocked on 003.
- **004 DONE ✓** — Executed at `1893eb5`. All 4 synchronous service calls in `router.py` wrapped with `asyncio.to_thread()`: `execute_graph_request` (line 36), `expand_node` (line 127), `get_collaboration_network` (line 257), `execute_and_format_query` (line 336). `grep -cn "asyncio.to_thread"` → 4. All 35 graph tests pass, 1 skipped (Neo4j disabled). No files outside scope modified.
- **005 TODO** — No drift. `worker.join()` at line 54 still has no timeout; `result_queue.get()` at line 53 still has no timeout. Code matches plan excerpts exactly. **Executable immediately (independent).**

## Dependency notes

- 003 depends on 002 (DONE ✓) — 003 is now unblocked and executable.
- 004 depends on 003 (DONE ✓) — **004 is now DONE ✓**.
- 001 and 005 are independent and can execute in parallel with everything else.

## Findings considered and rejected

- **DEBT-03 (f-string logging)**: Not worth a dedicated plan — low impact, high churn. Enable `pylint W1203` and fix incrementally.
- **DX-01 (.env.example duplication)**: Trivial fix; any developer can clean it up without a plan.
- **DX-02 (No AGENTS.md)**: Low priority; the project rules in `.github/copilot-instructions.md` serve the same purpose.
- **DIR-02 (Email connector stub)**: Feature work requiring product decision; out of scope for this audit.
- **BUG-04 / DEBT-01 (Person handler / process_single_pr duplication)**: Important refactors but lower leverage than security and performance fixes. Recommended for a future audit cycle.
- **SEC-03 (No auth on API)**: Critical but too large (L effort) for a single plan. Recommended as a dedicated project initiative.

## Audit reference

Full audit report: [audit.md](audit.md)
