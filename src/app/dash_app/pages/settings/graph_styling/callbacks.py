"""Callbacks for the Graph Styling settings page.

No callbacks are registered yet in Phase 4.1 (placeholder shell only). This
module exists so the package ``__init__.py`` can import it and register future
callbacks as the editor is built out in later phases.
"""

from __future__ import annotations
